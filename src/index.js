import React from 'react';
import ReactDOM from 'react-dom';
import RestaurantApp from './resto';

ReactDOM.render(
  <React.StrictMode>
    <RestaurantApp />
  </React.StrictMode>,
  document.getElementById('root')
);
